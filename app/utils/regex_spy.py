
import re

class RegexSpy:
    UP PATTERN = re.compile(r"")
    BANK_PATTERN = re.compile(r"")
    IP_PATTERN = re.compile(r"")

    @staticmethod
    def extract_opi(text):
        return []

    @staticmethod
    def extract_bank_details(text):
        return []
